// Find the unique values in the array of strings in O(n) time.

function unique(arr) {
    var unique = [];
    for (var i = 0; i < arr.length; i++) {
        


console.log(unique(["a", "b", "a", "b", "c", "d", "d"]));

